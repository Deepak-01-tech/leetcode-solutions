# 290. Word Pattern
  
<br>**Problem:** https://leetcode.com/problems/word-pattern/<br>

**Difficulty:** Easy<br>
**Topics:** Hash Table, String<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-09-24 10:38 local time

**Runtime:** 1 ms (beats 84.0481%)
**Memory:** 43 MB (beats 18.394800000000043%)


<!-- leetgit:submissionId=2151594878 codeHash=36eb99f1eaa53e94be7cede656d3fb96cdf61657ad57229d0301d2cbbe897919 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    public boolean wordPattern(String pattern, String s) {
        String[] words = s.split(" ");

        if (pattern.length() != words.length)
            return false;

        HashMap<Character, String> map = new HashMap<>();
        HashMap<String, Character> map2 = new HashMap<>();

        for (int i = 0; i < pattern.length(); i++) {
            char ch = pattern.charAt(i);
            String word = words[i];

            if (map.containsKey(ch) && !map.get(ch).equals(word))
                return false;

            if (map2.containsKey(word) && map2.get(word) != ch)
                return false;

            map.put(ch, word);
            map2.put(word, ch);
        }

        return true;
    }
}
```
